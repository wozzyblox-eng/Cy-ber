-- CYBER DUELS V2 - FIXED VERSION
-- I-paste lang ito sa executor at i-run

-- Wait for game to load
task.wait(1)

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Lighting = game:GetService("Lighting")
local player = Players.LocalPlayer
local camera = workspace.CurrentCamera

-- Colors
local ACCENT = Color3.fromRGB(0, 170, 255)
local WHITE = Color3.fromRGB(255, 255, 255)
local BG = Color3.fromRGB(12, 12, 22)
local CARD = Color3.fromRGB(22, 22, 38)
local OFF_CLR = Color3.fromRGB(38, 38, 55)

-- Variables
local NORMAL_SPEED = 59
local STEAL_SPEED = 29
local DESYNC_SPEED = 59
local DESYNC_STEAL_SPD = 29
local speedCustEnabled = true
local speedCustMode = "NORMAL"

local autoStealEnabled = false
local isStealing = false
local STEAL_RADIUS = 9
local STEAL_DURATION = 0.2

local antiRagdollEnabled = false
local unwalkEnabled = false
local batAimbotEnabled = false
local espEnabled = true
local optimizerEnabled = false
local slowDownEnabled = false
local tauntActive = false
local infJumpEnabled = true
local INF_JUMP_FORCE = 54

local aplOn = false
local aprOn = false
local medusaCounterEnabled = false

local holdingItem = false
local gChar, gHum, gHrp

local toggleStates = {}
local mobileButtons = {}
local mobBtnRefs = {}
local KeybindButtons = {}
local MainFrame = nil
local uiLocked = false

local CONFIG_KEY = "CYBER_DUELS_CONFIG"

-- Keybinds
local Keybinds = {
    AutoLeft = Enum.KeyCode.Q,
    AutoRight = Enum.KeyCode.E,
    AutoSteal = Enum.KeyCode.V,
    BatAimbot = Enum.KeyCode.Z,
    AntiRagdoll = Enum.KeyCode.X,
    Unwalk = Enum.KeyCode.N,
    SlowDown = Enum.KeyCode.F7,
    Drop = Enum.KeyCode.F3,
    Taunt = Enum.KeyCode.F4,
    TPDown = Enum.KeyCode.G,
}

-- Positions for Auto Play
local POS_L1 = Vector3.new(-476.48, -6.28, 92.73)
local POS_L2 = Vector3.new(-483.12, -4.95, 96.80)
local POS_R1 = Vector3.new(-476.16, -6.52, 25.62)
local POS_R2 = Vector3.new(-483.06, -5.11, 23.48)

-- ========== UTILITY FUNCTIONS ==========
local function getHRP()
    local c = player.Character
    return c and c:FindFirstChild("HumanoidRootPart")
end

local function getHum()
    local c = player.Character
    return c and c:FindFirstChildOfClass("Humanoid")
end

local function isHoldingItem()
    local char = player.Character
    if not char then return false end
    for _, obj in ipairs(char:GetChildren()) do
        if obj:IsA("Tool") then return true end
    end
    return false
end

-- ========== SAVE/LOAD CONFIG ==========
local function saveConfig()
    pcall(function()
        if not writefile then return end
        local data = {
            NORMAL_SPEED = NORMAL_SPEED,
            STEAL_RADIUS = STEAL_RADIUS,
            STEAL_DURATION = STEAL_DURATION,
            DESYNC_SPEED = DESYNC_SPEED,
            DESYNC_STEAL_SPD = DESYNC_STEAL_SPD,
            speedCustMode = speedCustMode,
            speedCustEnabled = speedCustEnabled,
        }
        for k, v in pairs(toggleStates) do
            if type(v) == "table" and v.state ~= nil then
                data["TOGGLE_" .. k] = v.state
            end
        end
        for k, v in pairs(Keybinds) do
            data["KEY_" .. k] = v.Name
        end
        writefile(CONFIG_KEY .. ".json", game:GetService("HttpService"):JSONEncode(data))
        print("[CYBER] Config saved!")
    end)
end

local function loadConfig()
    pcall(function()
        if not (readfile and isfile and isfile(CONFIG_KEY .. ".json")) then return end
        local data = game:GetService("HttpService"):JSONDecode(readfile(CONFIG_KEY .. ".json"))
        if data.NORMAL_SPEED then NORMAL_SPEED = data.NORMAL_SPEED end
        if data.STEAL_RADIUS then STEAL_RADIUS = data.STEAL_RADIUS end
        if data.STEAL_DURATION then STEAL_DURATION = data.STEAL_DURATION end
        if data.DESYNC_SPEED then DESYNC_SPEED = data.DESYNC_SPEED end
        if data.DESYNC_STEAL_SPD then DESYNC_STEAL_SPD = data.DESYNC_STEAL_SPD end
        if data.speedCustMode then speedCustMode = data.speedCustMode end
        if data.speedCustEnabled ~= nil then speedCustEnabled = data.speedCustEnabled end
        for k in pairs(Keybinds) do
            if data["KEY_" .. k] then
                pcall(function() Keybinds[k] = Enum.KeyCode[data["KEY_" .. k]] end)
            end
        end
        print("[CYBER] Config loaded!")
    end)
end

loadConfig()

-- ========== INF JUMP ==========
local spaceHeld = false

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.Space then spaceHeld = true end
end)

UserInputService.InputEnded:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.Space then spaceHeld = false end
end)

UserInputService.JumpRequest:Connect(function()
    if not infJumpEnabled then return end
    local h = getHRP()
    if h then
        h.AssemblyLinearVelocity = Vector3.new(h.AssemblyLinearVelocity.X, INF_JUMP_FORCE, h.AssemblyLinearVelocity.Z)
    end
end)

RunService.Heartbeat:Connect(function()
    if infJumpEnabled and spaceHeld then
        local h = getHRP()
        if h and h.AssemblyLinearVelocity.Y < INF_JUMP_FORCE then
            h.AssemblyLinearVelocity = Vector3.new(h.AssemblyLinearVelocity.X, INF_JUMP_FORCE, h.AssemblyLinearVelocity.Z)
        end
    end
end)

-- ========== DROP/FLING ==========
local wfActive = false

local function doDrop()
    if wfActive then return end
    wfActive = true
    local root = getHRP()
    if root then
        local burst = Vector3.new(math.random(-300, 300), 140, math.random(-300, 300))
        root.AssemblyLinearVelocity = burst
    end
    task.delay(0.3, function()
        local root2 = getHRP()
        if root2 then
            root2.AssemblyLinearVelocity = Vector3.new(0, -120, 0)
        end
        task.delay(0.1, function()
            local root3 = getHRP()
            if root3 then
                root3.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
            end
            wfActive = false
        end)
    end)
end

-- ========== TP DOWN ==========
local function doTPDown()
    local hrp = getHRP()
    local hum = getHum()
    if not hrp or not hum then return end
    local params = RaycastParams.new()
    params.FilterDescendantsInstances = {hrp.Parent}
    params.FilterType = Enum.RaycastFilterType.Exclude
    local result = workspace:Raycast(hrp.Position, Vector3.new(0, -1000, 0), params)
    if result then
        local y = result.Position.Y + hum.HipHeight + (hrp.Size.Y / 2)
        hrp.CFrame = CFrame.new(hrp.Position.X, y + 0.05, hrp.Position.Z)
    else
        hrp.AssemblyLinearVelocity = Vector3.new(0, -80, 0)
    end
end

-- ========== TAUNT ==========
local tauntLoop = nil

local function startTaunt()
    if tauntLoop then return end
    tauntActive = true
    tauntLoop = task.spawn(function()
        while tauntActive do
            pcall(function()
                local TCS = game:GetService("TextChatService")
                local ch = TCS.TextChannels:FindFirstChild("RBXGeneral")
                if ch then
                    ch:SendAsync("/e dance")
                else
                    local replicatedStorage = game:GetService("ReplicatedStorage")
                    local chatEvent = replicatedStorage:FindFirstChild("DefaultChatSystemChatEvents")
                    if chatEvent then
                        local sayMsg = chatEvent:FindFirstChild("SayMessageRequest")
                        if sayMsg then
                            sayMsg:FireServer("/e dance", "All")
                        end
                    end
                end
            end)
            task.wait(1)
        end
    end)
end

local function stopTaunt()
    tauntActive = false
    if tauntLoop then task.cancel(tauntLoop) tauntLoop = nil end
end

-- ========== SPEED CUSTOMIZATION ==========
RunService.Heartbeat:Connect(function()
    if not speedCustEnabled then return end
    holdingItem = isHoldingItem()
end)

RunService.Heartbeat:Connect(function()
    if not gChar or not gHum or not gHrp then return end
    if not batAimbotEnabled and not aplOn and not aprOn then
        local md = gHum.MoveDirection
        if md.Magnitude > 0.1 then
            local spd
            if speedCustEnabled and speedCustMode == "DESYNC" then
                if holdingItem or slowDownEnabled then
                    spd = DESYNC_STEAL_SPD
                else
                    spd = DESYNC_SPEED
                end
            else
                if holdingItem or slowDownEnabled then
                    spd = STEAL_SPEED
                else
                    spd = NORMAL_SPEED
                end
            end
            gHrp.AssemblyLinearVelocity = Vector3.new(md.X * spd, gHrp.AssemblyLinearVelocity.Y, md.Z * spd)
        end
    end
end)

-- ========== AUTO PLAY ==========
local aplConn = nil
local aprConn = nil
local aplPhase = 1
local aprPhase = 1

local function stopAutoPlayLeft()
    aplOn = false
    if aplConn then aplConn:Disconnect() aplConn = nil end
    aplPhase = 1
    local hh = getHum()
    if hh then hh:Move(Vector3.zero, false) end
    if updateToggle then updateToggle("Auto Play Left", false) end
end

local function stopAutoPlayRight()
    aprOn = false
    if aprConn then aprConn:Disconnect() aprConn = nil end
    aprPhase = 1
    local hh = getHum()
    if hh then hh:Move(Vector3.zero, false) end
    if updateToggle then updateToggle("Auto Play Right", false) end
end

local function startAutoPlayLeft()
    if aplConn then aplConn:Disconnect() end
    aplPhase = 1
    aplConn = RunService.Heartbeat:Connect(function()
        if not aplOn or not gHrp or not gHum then return end
        if aplPhase == 1 then
            local d = Vector3.new(POS_L1.X - gHrp.Position.X, 0, POS_L1.Z - gHrp.Position.Z)
            if d.Magnitude < 2 then
                aplPhase = 2
                return
            end
            local md = d.Unit
            gHum:Move(md, false)
            local currentSpeed = (speedCustEnabled and speedCustMode == "DESYNC") and DESYNC_SPEED or NORMAL_SPEED
            gHrp.AssemblyLinearVelocity = Vector3.new(md.X * currentSpeed, gHrp.AssemblyLinearVelocity.Y, md.Z * currentSpeed)
        elseif aplPhase == 2 then
            local d = Vector3.new(POS_L2.X - gHrp.Position.X, 0, POS_L2.Z - gHrp.Position.Z)
            if d.Magnitude < 2 then
                gHum:Move(Vector3.zero, false)
                gHrp.AssemblyLinearVelocity = Vector3.zero
                stopAutoPlayLeft()
                return
            end
            local md = d.Unit
            gHum:Move(md, false)
            local currentSpeed = (speedCustEnabled and speedCustMode == "DESYNC") and DESYNC_SPEED or NORMAL_SPEED
            gHrp.AssemblyLinearVelocity = Vector3.new(md.X * currentSpeed, gHrp.AssemblyLinearVelocity.Y, md.Z * currentSpeed)
        end
    end)
end

local function startAutoPlayRight()
    if aprConn then aprConn:Disconnect() end
    aprPhase = 1
    aprConn = RunService.Heartbeat:Connect(function()
        if not aprOn or not gHrp or not gHum then return end
        if aprPhase == 1 then
            local d = Vector3.new(POS_R1.X - gHrp.Position.X, 0, POS_R1.Z - gHrp.Position.Z)
            if d.Magnitude < 2 then
                aprPhase = 2
                return
            end
            local md = d.Unit
            gHum:Move(md, false)
            local currentSpeed = (speedCustEnabled and speedCustMode == "DESYNC") and DESYNC_SPEED or NORMAL_SPEED
            gHrp.AssemblyLinearVelocity = Vector3.new(md.X * currentSpeed, gHrp.AssemblyLinearVelocity.Y, md.Z * currentSpeed)
        elseif aprPhase == 2 then
            local d = Vector3.new(POS_R2.X - gHrp.Position.X, 0, POS_R2.Z - gHrp.Position.Z)
            if d.Magnitude < 2 then
                gHum:Move(Vector3.zero, false)
                gHrp.AssemblyLinearVelocity = Vector3.zero
                stopAutoPlayRight()
                return
            end
            local md = d.Unit
            gHum:Move(md, false)
            local currentSpeed = (speedCustEnabled and speedCustMode == "DESYNC") and DESYNC_SPEED or NORMAL_SPEED
            gHrp.AssemblyLinearVelocity = Vector3.new(md.X * currentSpeed, gHrp.AssemblyLinearVelocity.Y, md.Z * currentSpeed)
        end
    end)
end

-- ========== BAT AIMBOT ==========
local aimbotConnection = nil
local lockedTarget = nil
local AIMBOT_SPEED = 60
local MELEE_OFFSET = 1.5
local BAT_ENGAGE_RANGE = 5

local function isTargetValid(targetChar)
    if not targetChar then return false end
    local hum = targetChar:FindFirstChildOfClass("Humanoid")
    local hrp = targetChar:FindFirstChild("HumanoidRootPart")
    return hum and hrp and hum.Health > 0
end

local function getBestTarget(myHRP)
    if lockedTarget and isTargetValid(lockedTarget) then
        return lockedTarget:FindFirstChild("HumanoidRootPart"), lockedTarget
    end
    local shortestDist = math.huge
    local newTargetChar = nil
    local newTargetHRP = nil
    for _, targetPlayer in ipairs(Players:GetPlayers()) do
        if targetPlayer ~= player and isTargetValid(targetPlayer.Character) then
            local targetHRP = targetPlayer.Character:FindFirstChild("HumanoidRootPart")
            if targetHRP then
                local distance = (targetHRP.Position - myHRP.Position).Magnitude
                if distance < shortestDist then
                    shortestDist = distance
                    newTargetHRP = targetHRP
                    newTargetChar = targetPlayer.Character
                end
            end
        end
    end
    lockedTarget = newTargetChar
    return newTargetHRP, newTargetChar
end

local function findBatTool()
    local c = player.Character
    if not c then return nil end
    local bp = player:FindFirstChild("Backpack")
    for _, ch in ipairs(c:GetChildren()) do
        if ch:IsA("Tool") and (ch.Name:lower():find("bat") or ch.Name:lower():find("slap")) then
            return ch
        end
    end
    if bp then
        for _, ch in ipairs(bp:GetChildren()) do
            if ch:IsA("Tool") and (ch.Name:lower():find("bat") or ch.Name:lower():find("slap")) then
                return ch
            end
        end
    end
    return nil
end

local function startBatAimbot()
    if aimbotConnection then return end
    local c = player.Character
    if not c then return
    local h = c:FindFirstChild("HumanoidRootPart")
    local hum = c:FindFirstChildOfClass("Humanoid")
    if not h or not hum then return end
    
    hum.AutoRotate = false
    batAimbotEnabled = true
    
    aimbotConnection = RunService.Heartbeat:Connect(function()
        if not batAimbotEnabled then return end
        local currentChar = player.Character
        if not currentChar then return end
        
        local currentHRP = currentChar:FindFirstChild("HumanoidRootPart")
        local currentHum = currentChar:FindFirstChildOfClass("Humanoid")
        if not currentHRP or not currentHum then return end
        
        local bat = findBatTool()
        if bat and bat.Parent ~= currentChar then
            pcall(function() currentHum:EquipTool(bat) end)
        end
        
        local targetHRP, targetChar = getBestTarget(currentHRP)
        if targetHRP and targetChar then
            local targetVelocity = targetHRP.AssemblyLinearVelocity
            local predictedPos = targetHRP.Position + (targetVelocity * 0.1)
            local dirToTarget = (predictedPos - currentHRP.Position)
            local distance3D = dirToTarget.Magnitude
            local targetStandPos = predictedPos
            if distance3D > 0 then
                targetStandPos = predictedPos - (dirToTarget.Unit * MELEE_OFFSET)
            end
            
            local moveDir = (targetStandPos - currentHRP.Position)
            local distToStandPos = moveDir.Magnitude
            if distToStandPos > 1 then
                currentHRP.AssemblyLinearVelocity = moveDir.Unit * AIMBOT_SPEED
            else
                currentHRP.AssemblyLinearVelocity = targetVelocity
            end
            
            if distToStandPos <= BAT_ENGAGE_RANGE then
                if bat and bat.Parent == currentChar then
                    pcall(function() bat:Activate() end)
                end
            end
        else
            lockedTarget = nil
            currentHRP.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
        end
    end)
end

local function stopBatAimbot()
    batAimbotEnabled = false
    if aimbotConnection then
        aimbotConnection:Disconnect()
        aimbotConnection = nil
    end
    local c = player.Character
    local hum = c and c:FindFirstChildOfClass("Humanoid")
    if hum then hum.AutoRotate = true end
    lockedTarget = nil
end

-- ========== ANTI RAGDOLL ==========
local AntiRagdollConns = {}

local function startAntiRagdoll()
    if #AntiRagdollConns > 0 then return end
    local c = player.Character or player.CharacterAdded:Wait()
    local humanoid = c:WaitForChild("Humanoid")
    
    local function CleanRagdollEffects()
        for _, obj in pairs(c:GetDescendants()) do
            if obj:IsA("BodyVelocity") or obj:IsA("BodyPosition") or obj:IsA("BodyGyro") then
                obj:Destroy()
            end
        end
    end
    
    local function IsRagdollState()
        local s = humanoid:GetState()
        return s == Enum.HumanoidStateType.Physics or s == Enum.HumanoidStateType.Ragdoll
    end
    
    table.insert(AntiRagdollConns, humanoid.StateChanged:Connect(function()
        if IsRagdollState() then
            humanoid:ChangeState(Enum.HumanoidStateType.Running)
            CleanRagdollEffects()
            workspace.CurrentCamera.CameraSubject = humanoid
        end
    end))
    
    table.insert(AntiRagdollConns, player.CharacterAdded:Connect(function(newChar)
        c = newChar
        humanoid = c:WaitForChild("Humanoid")
        CleanRagdollEffects()
    end))
end

local function stopAntiRagdoll()
    for _, conn in pairs(AntiRagdollConns) do
        conn:Disconnect()
    end
    AntiRagdollConns = {}
end

-- ========== UNWALK ==========
local unwalkConn = nil

local function startUnwalk()
    if unwalkConn then return end
    unwalkConn = RunService.Heartbeat:Connect(function()
        if not unwalkEnabled then return end
        local c = player.Character
        if not c then return end
        local hh = c:FindFirstChildOfClass("Humanoid")
        if not hh then return end
        local an = hh:FindFirstChildOfClass("Animator")
        if not an then return end
        for _, t in ipairs(an:GetPlayingAnimationTracks()) do
            t:Stop(0)
        end
    end)
end

local function stopUnwalk()
    if unwalkConn then
        unwalkConn:Disconnect()
        unwalkConn = nil
    end
end

-- ========== ESP ==========
local espConns = {}

local function createESP(plr)
    if plr == player or not plr.Character then return end
    local c = plr.Character
    local root = c:FindFirstChild("HumanoidRootPart")
    local head = c:FindFirstChild("Head")
    if not root or not head then return end
    if c:FindFirstChild("CyberESP") then return end
    
    local box = Instance.new("BoxHandleAdornment")
    box.Name = "CyberESP"
    box.Adornee = root
    box.Size = Vector3.new(4, 6, 2)
    box.Color3 = ACCENT
    box.Transparency = 0.45
    box.ZIndex = 10
    box.AlwaysOnTop = true
    box.Parent = c
    
    local bb = Instance.new("BillboardGui")
    bb.Name = "CyberESP_Name"
    bb.Adornee = head
    bb.Size = UDim2.new(0, 200, 0, 45)
    bb.StudsOffset = Vector3.new(0, 3, 0)
    bb.AlwaysOnTop = true
    bb.Parent = c
    
    local lbl = Instance.new("TextLabel")
    lbl.Size = UDim2.new(1, 0, 1, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = plr.DisplayName
    lbl.TextColor3 = WHITE
    lbl.Font = Enum.Font.GothamBold
    lbl.TextScaled = true
    lbl.TextStrokeTransparency = 0.5
    lbl.TextStrokeColor3 = ACCENT
    lbl.Parent = bb
end

local function removeESP(plr)
    if not plr.Character then return end
    local b = plr.Character:FindFirstChild("CyberESP")
    local n = plr.Character:FindFirstChild("CyberESP_Name")
    if b then b:Destroy() end
    if n then n:Destroy() end
end

local function enableESP()
    for _, plr in ipairs(Players:GetPlayers()) do
        if plr ~= player then
            if plr.Character then pcall(function() createESP(plr) end) end
            table.insert(espConns, plr.CharacterAdded:Connect(function()
                task.wait(0.1)
                if espEnabled then pcall(function() createESP(plr) end) end
            end))
        end
    end
    
    table.insert(espConns, Players.PlayerAdded:Connect(function(plr)
        if plr == player then return end
        table.insert(espConns, plr.CharacterAdded:Connect(function()
            task.wait(0.1)
            if espEnabled then pcall(function() createESP(plr) end) end
        end))
    end))
end

local function disableESP()
    for _, plr in ipairs(Players:GetPlayers()) do
        pcall(function() removeESP(plr) end)
    end
    for _, c in ipairs(espConns) do
        if c and c.Connected then c:Disconnect() end
    end
    espConns = {}
end

-- ========== OPTIMIZER ==========
local optimizerDescConn = nil

local function enableOptimizer()
    pcall(function()
        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
        Lighting.GlobalShadows = false
        Lighting.Brightness = 2
        Lighting.FogEnd = 9e9
        Lighting.FogStart = 9e9
        Lighting.ClockTime = 8
        Lighting.Ambient = Color3.fromRGB(180, 180, 255)
        Lighting.OutdoorAmbient = Color3.fromRGB(180, 180, 255)
        
        for _, fx in ipairs(Lighting:GetChildren()) do
            if fx:IsA("PostEffect") or fx:IsA("Atmosphere") or fx:IsA("Sky") then
                pcall(function() fx:Destroy() end)
            end
        end
    end)
    
    pcall(function()
        if optimizerDescConn then optimizerDescConn:Disconnect() end
        optimizerDescConn = workspace.DescendantAdded:Connect(function(obj)
            if not optimizerEnabled then return end
            pcall(function()
                if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") then
                    obj:Destroy()
                end
            end)
        end)
    end)
end

local function disableOptimizer()
    pcall(function()
        settings().Rendering.QualityLevel = Enum.QualityLevel.Automatic
        Lighting.GlobalShadows = true
        Lighting.Brightness = 2
        Lighting.FogEnd = 100000
        Lighting.FogStart = 0
        Lighting.ClockTime = 14
        Lighting.Ambient = Color3.fromRGB(127, 127, 127)
        Lighting.OutdoorAmbient = Color3.fromRGB(127, 127, 127)
    end)
    if optimizerDescConn then
        optimizerDescConn:Disconnect()
        optimizerDescConn = nil
    end
end

-- ========== CHARACTER SETUP ==========
local function setupChar(c)
    gChar = c
    gHum = c:WaitForChild("Humanoid", 5)
    gHrp = c:WaitForChild("HumanoidRootPart", 5)
    if not gHum or not gHrp then return end
    
    task.wait(0.5)
    
    if antiRagdollEnabled then
        stopAntiRagdoll()
        startAntiRagdoll()
    end
    if espEnabled then enableESP() end
    if batAimbotEnabled then
        stopBatAimbot()
        startBatAimbot()
    end
    if unwalkEnabled then startUnwalk() end
end

if player.Character then
    setupChar(player.Character)
end

player.CharacterAdded:Connect(function(c)
    task.wait(0.5)
    setupChar(c)
end)

-- ========== GUI CONSTRUCTION ==========
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "CYBER_HUB"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = player:WaitForChild("PlayerGui")

MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 240, 0, 500)
MainFrame.Position = UDim2.new(0.5, -120, 0.5, -250)
MainFrame.BackgroundColor3 = BG
MainFrame.BorderSizePixel = 0
MainFrame.Active = true
MainFrame.Visible = false
MainFrame.Parent = ScreenGui
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 16)

local MainStroke = Instance.new("UIStroke", MainFrame)
MainStroke.Thickness = 1.5
MainStroke.Color = ACCENT
MainStroke.Transparency = 0.3

-- Make draggable
do
    local dragging, dragStart, startPos
    MainFrame.InputBegan:Connect(function(input)
        if uiLocked then return end
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = MainFrame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if uiLocked then return end
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            MainFrame.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end)
end

-- Title Bar
local TitleBar = Instance.new("Frame", MainFrame)
TitleBar.Size = UDim2.new(1, 0, 0, 48)
TitleBar.BackgroundColor3 = Color3.fromRGB(18, 14, 32)
TitleBar.BorderSizePixel = 0
Instance.new("UICorner", TitleBar).CornerRadius = UDim.new(0, 16)

local TitleFix = Instance.new("Frame", TitleBar)
TitleFix.Size = UDim2.new(1, 0, 0, 12)
TitleFix.Position = UDim2.new(0, 0, 1, -12)
TitleFix.BackgroundColor3 = Color3.fromRGB(18, 14, 32)
TitleFix.BorderSizePixel = 0

local TitleLbl = Instance.new("TextLabel", TitleBar)
TitleLbl.Size = UDim2.new(1, -100, 1, 0)
TitleLbl.Position = UDim2.new(0, 48, 0, 0)
TitleLbl.BackgroundTransparency = 1
TitleLbl.Text = "CYBER DUELS"
TitleLbl.Font = Enum.Font.GothamBlack
TitleLbl.TextSize = 14
TitleLbl.TextColor3 = WHITE
TitleLbl.TextStrokeColor3 = ACCENT
TitleLbl.TextStrokeTransparency = 0.4
TitleLbl.TextXAlignment = Enum.TextXAlignment.Left

-- Close button
local CloseBtn = Instance.new("TextButton", TitleBar)
CloseBtn.Size = UDim2.new(0, 30, 0, 30)
CloseBtn.Position = UDim2.new(1, -40, 0.5, -15)
CloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
CloseBtn.Text = "X"
CloseBtn.Font = Enum.Font.GothamBlack
CloseBtn.TextSize = 14
CloseBtn.TextColor3 = WHITE
CloseBtn.BorderSizePixel = 0
Instance.new("UICorner", CloseBtn).CornerRadius = UDim.new(0, 8)
CloseBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = false
end)

-- Scroll Frame
local ScrollFrame = Instance.new("ScrollingFrame", MainFrame)
ScrollFrame.Size = UDim2.new(1, -16, 1, -78)
ScrollFrame.Position = UDim2.new(0, 8, 0, 52)
ScrollFrame.BackgroundTransparency = 1
ScrollFrame.ScrollBarThickness = 3
ScrollFrame.ScrollBarImageColor3 = ACCENT
ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
ScrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y

local ListLayout = Instance.new("UIListLayout", ScrollFrame)
ListLayout.Padding = UDim.new(0, 6)
ListLayout.SortOrder = Enum.SortOrder.LayoutOrder

-- Helper functions
local function makeSection(text, order)
    local lbl = Instance.new("TextLabel", ScrollFrame)
    lbl.Size = UDim2.new(1, 0, 0, 24)
    lbl.BackgroundTransparency = 1
    lbl.Text = "  " .. text
    lbl.TextColor3 = ACCENT
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 10
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    lbl.LayoutOrder = order
end

function updateToggle(name, state)
    local t = toggleStates[name]
    if not t then return end
    t.state = state
    t.track.BackgroundColor3 = state and ACCENT or OFF_CLR
    t.dot.Position = state and UDim2.new(1, -t.dotSz - 3, 0.5, -t.dotSz / 2) or UDim2.new(0, 3, 0.5, -t.dotSz / 2)
    if mobBtnRefs[name] then
        local btn = mobBtnRefs[name]
        if state then
            btn.BackgroundColor3 = Color3.fromRGB(0, 50, 100)
            btn.TextColor3 = WHITE
        else
            btn.BackgroundColor3 = Color3.fromRGB(8, 8, 16)
            btn.TextColor3 = ACCENT
        end
    end
end

local function makeToggle(name, order, callback, defaultOn)
    local row = Instance.new("Frame", ScrollFrame)
    row.Size = UDim2.new(1, 0, 0, 42)
    row.BackgroundColor3 = CARD
    row.BorderSizePixel = 0
    row.LayoutOrder = order
    Instance.new("UICorner", row).CornerRadius = UDim.new(0, 10)
    
    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(1, -70, 1, 0)
    lbl.Position = UDim2.new(0, 14, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = name
    lbl.TextColor3 = WHITE    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 11
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    
    local pW, pH, dSz = 44, 22, 16
    local track = Instance.new("Frame", row)
    track.Size = UDim2.new(0, pW, 0, pH)
    track.Position = UDim2.new(1, -(pW + 12), 0.5, -pH / 2)
    track.BackgroundColor3 = OFF_CLR
    track.BorderSizePixel = 0
    Instance.new("UICorner", track).CornerRadius = UDim.new(1, 0)
    
    local dot = Instance.new("Frame", track)
    dot.Size = UDim2.new(0, dSz, 0, dSz)
    dot.Position = UDim2.new(0, 3, 0.5, -dSz / 2)
    dot.BackgroundColor3 = WHITE
    dot.BorderSizePixel = 0
    Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)
    
    toggleStates[name] = { track = track, dot = dot, state = false, dotSz = dSz }
    
    local btn = Instance.new("TextButton", row)
    btn.Size = UDim2.new(1, 0, 1, 0)
    btn.BackgroundTransparency = 1
    btn.Text = ""
    btn.MouseButton1Click:Connect(function()
        local ns = not toggleStates[name].state
        toggleStates[name].state = ns
        track.BackgroundColor3 = ns and ACCENT or OFF_CLR
        dot.Position = ns and UDim2.new(1, -dSz - 3, 0.5, -dSz / 2) or UDim2.new(0, 3, 0.5, -dSz / 2)
        if callback then callback(ns) end
        task.defer(saveConfig)
    end)
    return row
end

local function makeNumInput(labelText, defaultVal, order, onChanged)
    local row = Instance.new("Frame", ScrollFrame)
    row.Size = UDim2.new(1, 0, 0, 42)
    row.BackgroundColor3 = CARD
    row.BorderSizePixel = 0
    row.LayoutOrder = order
    Instance.new("UICorner", row).CornerRadius = UDim.new(0, 10)
    
    local lbl = Instance.new("TextLabel", row)
    lbl.Size = UDim2.new(1, -80, 1, 0)
    lbl.Position = UDim2.new(0, 14, 0, 0)
    lbl.BackgroundTransparency = 1
    lbl.Text = labelText
    lbl.TextColor3 = WHITE
    lbl.Font = Enum.Font.GothamBold
    lbl.TextSize = 11
    lbl.TextXAlignment = Enum.TextXAlignment.Left
    
    local box = Instance.new("TextBox", row)
    box.Size = UDim2.new(0, 60, 0, 26)
    box.Position = UDim2.new(1, -68, 0.5, -13)
    box.BackgroundColor3 = Color3.fromRGB(28, 26, 44)
    box.Text = tostring(defaultVal)
    box.TextColor3 = ACCENT
    box.Font = Enum.Font.GothamBold
    box.TextSize = 11
    box.TextXAlignment = Enum.TextXAlignment.Center
    box.BorderSizePixel = 0
    box.ClearTextOnFocus = false
    Instance.new("UICorner", box).CornerRadius = UDim.new(0, 8)
    
    box.FocusLost:Connect(function()
        local n = tonumber(box.Text)
        if n then
            if onChanged then onChanged(n) end
            task.defer(saveConfig)
        else
            box.Text = tostring(defaultVal)
        end
    end)
    return row, box
end

local function makeActionBtn(label, order, onClick)
    local btn = Instance.new("TextButton", ScrollFrame)
    btn.Size = UDim2.new(1, 0, 0, 36)
    btn.BackgroundColor3 = Color3.fromRGB(25, 20, 45)
    btn.Text = label
    btn.Font = Enum.Font.GothamBlack
    btn.TextSize = 11
    btn.TextColor3 = ACCENT
    btn.BorderSizePixel = 0
    btn.LayoutOrder = order
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)
    btn.MouseButton1Click:Connect(function() task.spawn(onClick) end)
    return btn
end

-- Build GUI
makeSection("COMBAT", 1)
makeToggle("Bat Aimbot", 2, function(v)
    if v then
        if aplOn then stopAutoPlayLeft() end
        if aprOn then stopAutoPlayRight() end
        startBatAimbot()
    else
        stopBatAimbot()
    end
end)
makeToggle("Anti Ragdoll", 3, function(v)
    antiRagdollEnabled = v
    if v then startAntiRagdoll() else stopAntiRagdoll() end
end)
makeToggle("Unwalk", 4, function(v)
    unwalkEnabled = v
    if v then startUnwalk() else stopUnwalk() end
end)

makeSection("MOVEMENT", 10)
makeToggle("Auto Play Left", 11, function(v)
    if v then
        if batAimbotEnabled then stopBatAimbot() updateToggle("Bat Aimbot", false) end
        if aprOn then stopAutoPlayRight() end
        aplOn = true
        startAutoPlayLeft()
    else
        stopAutoPlayLeft()
    end
end)
makeToggle("Auto Play Right", 12, function(v)
    if v then
        if batAimbotEnabled then stopBatAimbot() updateToggle("Bat Aimbot", false) end
        if aplOn then stopAutoPlayLeft() end
        aprOn = true
        startAutoPlayRight()
    else
        stopAutoPlayRight()
    end
end)
makeToggle("Slow Down", 13, function(v) slowDownEnabled = v end)

makeSection("VISUALS", 30)
makeToggle("Player ESP", 31, function(v)
    espEnabled = v
    if v then enableESP() else disableESP() end
end, true)
makeToggle("Optimizer", 32, function(v)
    optimizerEnabled = v
    if v then enableOptimizer() else disableOptimizer() end
end)

makeSection("ACTIONS", 40)
makeActionBtn("DO DROP", 41, doDrop)
makeActionBtn("TP DOWN", 42, doTPDown)
makeToggle("Taunt Spam", 43, function(v)
    if v then startTaunt() else stopTaunt() end
end)

makeSection("CONFIG", 50)
makeToggle("Lock UI", 51, function(v) uiLocked = v end)

-- Save Config Button
local saveBtn = Instance.new("TextButton", ScrollFrame)
saveBtn.Size = UDim2.new(1, 0, 0, 32)
saveBtn.BackgroundColor3 = Color3.fromRGB(20, 60, 35)
saveBtn.Text = "SAVE CONFIG"
saveBtn.Font = Enum.Font.GothamBlack
saveBtn.TextSize = 11
saveBtn.TextColor3 = WHITE
saveBtn.BorderSizePixel = 0
saveBtn.LayoutOrder = 52
saveBtn.AutoButtonColor = false
Instance.new("UICorner", saveBtn).CornerRadius = UDim.new(0, 10)
saveBtn.MouseButton1Click:Connect(function()
    saveConfig()
    saveBtn.Text = "SAVED!"
    saveBtn.TextColor3 = Color3.fromRGB(100, 255, 140)
    task.delay(1.5, function()
        saveBtn.Text = "SAVE CONFIG"
        saveBtn.TextColor3 = WHITE
    end)
end)

-- Reset Config Button
local resetBtn = Instance.new("TextButton", ScrollFrame)
resetBtn.Size = UDim2.new(1, 0, 0, 32)
resetBtn.BackgroundColor3 = Color3.fromRGB(35, 8, 8)
resetBtn.Text = "RESET CONFIG"
resetBtn.Font = Enum.Font.GothamBlack
resetBtn.TextSize = 11
resetBtn.TextColor3 = Color3.fromRGB(255, 80, 80)
resetBtn.BorderSizePixel = 0
resetBtn.LayoutOrder = 53
resetBtn.AutoButtonColor = false
Instance.new("UICorner", resetBtn).CornerRadius = UDim.new(0, 10)

local resetArmed = false
resetBtn.MouseButton1Click:Connect(function()
    if not resetArmed then
        resetArmed = true
        resetBtn.Text = "ARE YOU SURE?"
        resetBtn.TextColor3 = Color3.fromRGB(255, 200, 50)
        task.delay(2, function()
            resetArmed = false
            resetBtn.Text = "RESET CONFIG"
            resetBtn.TextColor3 = Color3.fromRGB(255, 80, 80)
        end)
    else
        resetArmed = false
        
        -- Reset values
        NORMAL_SPEED = 59
        STEAL_RADIUS = 9
        STEAL_DURATION = 0.2
        DESYNC_SPEED = 59
        DESYNC_STEAL_SPD = 29
        speedCustMode = "NORMAL"
        speedCustEnabled = true
        
        -- Stop all features
        if batAimbotEnabled then stopBatAimbot() end
        if antiRagdollEnabled then stopAntiRagdoll() end
        if unwalkEnabled then stopUnwalk() end
        if aplOn then stopAutoPlayLeft() end
        if aprOn then stopAutoPlayRight() end
        if tauntActive then stopTaunt() end
        
        -- Reset toggles
        for name, t in pairs(toggleStates) do
            if t.state then
                t.state = false
                t.track.BackgroundColor3 = OFF_CLR
                t.dot.Position = UDim2.new(0, 3, 0.5, -t.dotSz / 2)
            end
        end
        
        -- Reset flags
        uiLocked = false
        slowDownEnabled = false
        batAimbotEnabled = false
        antiRagdollEnabled = false
        unwalkEnabled = false
        
        -- Reset mobile buttons
        if mobBtnRefs["Auto Play Left"] then
            mobBtnRefs["Auto Play Left"].BackgroundColor3 = Color3.fromRGB(8, 8, 16)
            mobBtnRefs["Auto Play Left"].TextColor3 = ACCENT
        end
        if mobBtnRefs["Auto Play Right"] then
            mobBtnRefs["Auto Play Right"].BackgroundColor3 = Color3.fromRGB(8, 8, 16)
            mobBtnRefs["Auto Play Right"].TextColor3 = ACCENT
        end
        if mobBtnRefs["Bat Aimbot"] then
            mobBtnRefs["Bat Aimbot"].BackgroundColor3 = Color3.fromRGB(8, 8, 16)
            mobBtnRefs["Bat Aimbot"].TextColor3 = ACCENT
        end
        if mobBtnRefs["Slow Down"] then
            mobBtnRefs["Slow Down"].BackgroundColor3 = Color3.fromRGB(8, 8, 16)
            mobBtnRefs["Slow Down"].TextColor3 = ACCENT
        end
        
        -- Delete config file
        pcall(function()
            if delfile then delfile(CONFIG_KEY .. ".json") end
        end)
        
        resetBtn.Text = "RESET DONE!"
        resetBtn.TextColor3 = Color3.fromRGB(100, 255, 140)
        task.delay(2, function()
            resetBtn.Text = "RESET CONFIG"
            resetBtn.TextColor3 = Color3.fromRGB(255, 80, 80)
        end)
        
        print("[CYBER] Config reset to default!")
    end
end)

makeSection("KEYBINDS", 60)
local hint = Instance.new("TextLabel", ScrollFrame)
hint.Size = UDim2.new(1, 0, 0, 28)
hint.BackgroundColor3 = Color3.fromRGB(20, 12, 35)
hint.Text = "Click a key to rebind. CTRL = clear"
hint.TextColor3 = Color3.fromRGB(180, 160, 220)
hint.Font = Enum.Font.Gotham
hint.TextSize = 9
hint.TextWrapped = true
hint.LayoutOrder = 61
Instance.new("UICorner", hint).CornerRadius = UDim.new(0, 10)

local bindList = {
    {"Auto Left", "AutoLeft"}, {"Auto Right", "AutoRight"}, {"Auto Steal", "AutoSteal"},
    {"Bat Aimbot", "BatAimbot"}, {"Anti Ragdoll", "AntiRagdoll"}, {"Unwalk", "Unwalk"},
    {"Slow Down", "SlowDown"}, {"Drop", "Drop"}, {"Taunt", "Taunt"}, {"TP Down", "TPDown"}
}

local changingKeybind = nil

for idx, entry in ipairs(bindList) do
    local displayName, keyName = entry[1], entry[2]
    local row = Instance.new("Frame", ScrollFrame)
    row.Size = UDim2.new(1, 0, 0, 42)
    row.BackgroundColor3 = CARD
    row.BorderSizePixel = 0
    row.LayoutOrder = 61 + idx
    Instance.new("UICorner", row).CornerRadius = UDim.new(0, 10)
    
    local nameLbl = Instance.new("TextLabel", row)
    nameLbl.Size = UDim2.new(1, -90, 1, 0)
    nameLbl.Position = UDim2.new(0, 12, 0, 0)
    nameLbl.BackgroundTransparency = 1
    nameLbl.Text = displayName
    nameLbl.TextColor3 = WHITE
    nameLbl.Font = Enum.Font.GothamBold
    nameLbl.TextSize = 10
    nameLbl.TextXAlignment = Enum.TextXAlignment.Left
    
    local keyBtn = Instance.new("TextButton", row)
    keyBtn.Size = UDim2.new(0, 75, 0, 26)
    keyBtn.Position = UDim2.new(1, -82, 0.5, -13)
    keyBtn.BackgroundColor3 = Color3.fromRGB(30, 20, 50)
    keyBtn.Text = tostring(Keybinds[keyName]):gsub("Enum.KeyCode.", "")
    keyBtn.Font = Enum.Font.GothamBold
    keyBtn.TextSize = 9
    keyBtn.TextColor3 = ACCENT
    keyBtn.BorderSizePixel = 0
    Instance.new("UICorner", keyBtn).CornerRadius = UDim.new(0, 8)
    KeybindButtons[keyName] = keyBtn
    
    keyBtn.MouseButton1Click:Connect(function()
        if changingKeybind then return end
        changingKeybind = keyName
        keyBtn.Text = "..."
        keyBtn.TextColor3 = Color3.fromRGB(255, 200, 50)
        local conn
        conn = UserInputService.InputBegan:Connect(function(input)
            if input.UserInputType == Enum.UserInputType.Keyboard then
                if input.KeyCode == Enum.KeyCode.LeftControl or input.KeyCode == Enum.KeyCode.RightControl then
                    Keybinds[keyName] = nil
                    keyBtn.Text = "NONE"
                elseif input.KeyCode ~= Enum.KeyCode.Unknown then
                    Keybinds[keyName] = input.KeyCode
                    keyBtn.Text = tostring(input.KeyCode):gsub("Enum.KeyCode.", "")
                end
                keyBtn.TextColor3 = ACCENT
                changingKeybind = nil
                conn:Disconnect()
                task.defer(saveConfig)
            end
        end)
    end)
end

-- Speed Widget
do
    local SCGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
    SCGui.Name = "CyberSpeedWidget"
    SCGui.ResetOnSpawn = false
    
    local SCFrame = Instance.new("Frame", SCGui)
    SCFrame.Size = UDim2.new(0, 160, 0, 100)
    SCFrame.Position = UDim2.new(0, 10, 0.3, 0)
    SCFrame.BackgroundColor3 = Color3.fromRGB(8, 8, 16)
    SCFrame.BorderSizePixel = 0
    Instance.new("UICorner", SCFrame).CornerRadius = UDim.new(0, 12)
    
    local SCTitle = Instance.new("Frame", SCFrame)
    SCTitle.Size = UDim2.new(1, 0, 0, 24)
    SCTitle.BackgroundColor3 = Color3.fromRGB(12, 10, 24)
    SCTitle.BorderSizePixel = 0
    Instance.new("UICorner", SCTitle).CornerRadius = UDim.new(0, 12)
    
    local SCTitleLbl = Instance.new("TextLabel", SCTitle)
    SCTitleLbl.Size = UDim2.new(1, -60, 1, 0)
    SCTitleLbl.Position = UDim2.new(0, 10, 0, 0)
    SCTitleLbl.BackgroundTransparency = 1
    SCTitleLbl.Text = "SPEED"
    SCTitleLbl.Font = Enum.Font.GothamBlack
    SCTitleLbl.TextSize = 10
    SCTitleLbl.TextColor3 = WHITE
    SCTitleLbl.TextXAlignment = Enum.TextXAlignment.Left
    
    local onOffBtn = Instance.new("TextButton", SCFrame)
    onOffBtn.Size = UDim2.new(1, -16, 0, 26)
    onOffBtn.Position = UDim2.new(0, 8, 0, 30)
    onOffBtn.BackgroundColor3 = speedCustEnabled and Color3.fromRGB(0, 50, 100) or Color3.fromRGB(8, 8, 16)
    onOffBtn.Text = speedCustEnabled and "ON" or "OFF"
    onOffBtn.Font = Enum.Font.GothamBold
    onOffBtn.TextSize = 11
    onOffBtn.TextColor3 = speedCustEnabled and WHITE or ACCENT
    onOffBtn.BorderSizePixel = 0
    Instance.new("UICorner", onOffBtn).CornerRadius = UDim.new(0, 8)
    onOffBtn.MouseButton1Click:Connect(function()
        speedCustEnabled = not speedCustEnabled
        onOffBtn.BackgroundColor3 = speedCustEnabled and Color3.fromRGB(0, 50, 100) or Color3.fromRGB(8, 8, 16)
        onOffBtn.Text = speedCustEnabled and "ON" or "OFF"
        onOffBtn.TextColor3 = speedCustEnabled and WHITE or ACCENT
        saveConfig()
    end)
    
    local tabBar = Instance.new("Frame", SCFrame)
    tabBar.Size = UDim2.new(1, -16, 0, 28)
    tabBar.Position = UDim2.new(0, 8, 0, 62)
    tabBar.BackgroundColor3 = Color3.fromRGB(18, 16, 30)
    tabBar.BorderSizePixel = 0
    Instance.new("UICorner", tabBar).CornerRadius = UDim.new(0, 8)
    
    local normalBtn = Instance.new("TextButton", tabBar)
    normalBtn.Size = UDim2.new(0.5, -4, 1, -4)
    normalBtn.Position = UDim2.new(0, 2, 0, 2)
    normalBtn.BackgroundColor3 = speedCustMode == "NORMAL" and ACCENT or Color3.fromRGB(18, 16, 30)
    normalBtn.Text = "NORMAL"
    normalBtn.Font = Enum.Font.GothamBold
    normalBtn.TextSize = 10
    normalBtn.TextColor3 = speedCustMode == "NORMAL" and WHITE or ACCENT
    normalBtn.BorderSizePixel = 0
    Instance.new("UICorner", normalBtn).CornerRadius = UDim.new(0, 6)
    
    local desyncBtn = Instance.new("TextButton", tabBar)
    desyncBtn.Size = UDim2.new(0.5, -4, 1, -4)
    desyncBtn.Position = UDim2.new(0.5, 2, 0, 2)
    desyncBtn.BackgroundColor3 = speedCustMode == "DESYNC" and ACCENT or Color3.fromRGB(18, 16, 30)
    desyncBtn.Text = "DESYNC"
    desyncBtn.Font = Enum.Font.GothamBold
    desyncBtn.TextSize = 10
    desyncBtn.TextColor3 = speedCustMode == "DESYNC" and WHITE or ACCENT
    desyncBtn.BorderSizePixel = 0
    Instance.new("UICorner", desyncBtn).CornerRadius = UDim.new(0, 6)
    
    normalBtn.MouseButton1Click:Connect(function()
        speedCustMode = "NORMAL"
        normalBtn.BackgroundColor3 = ACCENT
        normalBtn.TextColor3 = WHITE
        desyncBtn.BackgroundColor3 = Color3.fromRGB(18, 16, 30)
        desyncBtn.TextColor3 = ACCENT
        saveConfig()
    end)
    
    desyncBtn.MouseButton1Click:Connect(function()
        speedCustMode = "DESYNC"
        desyncBtn.BackgroundColor3 = ACCENT
        desyncBtn.TextColor3 = WHITE
        normalBtn.BackgroundColor3 = Color3.fromRGB(18, 16, 30)
        normalBtn.TextColor3 = ACCENT
        saveConfig()
    end)
    
    -- Make widget draggable
    local dragging = false
    local dragStart = nil
    local startPos = nil
    
    SCTitle.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = SCFrame.Position
        end
    end)
    
    SCTitle.InputEnded:Connect(function()
        dragging = false
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            SCFrame.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end)
end

-- Mobile Buttons
local MOB_W = 76
local MOB_H = 32

local function makeMob(label, pos, toggleName, onAct)
    local btn = Instance.new("TextButton", ScreenGui)
    btn.Size = UDim2.new(0, MOB_W, 0, MOB_H)
    btn.Position = pos
    btn.BackgroundColor3 = Color3.fromRGB(8, 8, 16)
    btn.Text = label
    btn.TextColor3 = ACCENT
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 10
    btn.BorderSizePixel = 0
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 10)
    table.insert(mobileButtons, btn)
    if toggleName then mobBtnRefs[toggleName] = btn end
    
    -- Make draggable
    local dragging = false
    local dragStart = nil
    local startPos = nil
    
    btn.InputBegan:Connect(function(input)
        if uiLocked then return end
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = btn.Position
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if uiLocked then return end
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            btn.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end)
    
    UserInputService.InputEnded:Connect(function()
        dragging = false
    end)
    
    btn.MouseButton1Click:Connect(onAct)
    return btn
end

makeMob("Drop", UDim2.new(1, -(MOB_W + 8), 0.42, 0), nil, function() task.spawn(doDrop) end)
makeMob("Auto L", UDim2.new(1, -(MOB_W + 8), 0.49, 0), "Auto Play Left", function()
    local ns = not aplOn
    if ns then
        if batAimbotEnabled then stopBatAimbot() updateToggle("Bat Aimbot", false) end
        if aprOn then stopAutoPlayRight() end
        aplOn = true
        startAutoPlayLeft()
    else
        stopAutoPlayLeft()
    end
    updateToggle("Auto Play Left", ns)
end)
makeMob("Auto Bat", UDim2.new(1, -(MOB_W + 8), 0.56, 0), "Bat Aimbot", function()
    local ns = not batAimbotEnabled
    if ns then
        if aplOn then stopAutoPlayLeft() end
        if aprOn then stopAutoPlayRight() end
        startBatAimbot()
    else
        stopBatAimbot()
    end
    updateToggle("Bat Aimbot", ns)
end)
makeMob("Auto R", UDim2.new(1, -(MOB_W + 8), 0.63, 0), "Auto Play Right", function()
    local ns = not aprOn
    if ns then
        if batAimbotEnabled then stopBatAimbot() updateToggle("Bat Aimbot", false) end
        if aplOn then stopAutoPlayLeft() end
        aprOn = true
        startAutoPlayRight()
    else
        stopAutoPlayRight()
    end
    updateToggle("Auto Play Right", ns)
end)
makeMob("Slow", UDim2.new(1, -(MOB_W + 8), 0.70, 0), "Slow Down", function()
    local ns = not slowDownEnabled
    slowDownEnabled = ns
    updateToggle("Slow Down", ns)
end)
makeMob("TP Down", UDim2.new(1, -(MOB_W + 8), 0.77, 0), nil, function() task.spawn(doTPDown) end)
makeMob("Taunt", UDim2.new(1, -(MOB_W + 8), 0.84, 0), nil, function()
    if not tauntActive then startTaunt() else stopTaunt() end
end)

-- Open/Close Button
do
    local OCGui = Instance.new("ScreenGui", player:WaitForChild("PlayerGui"))
    OCGui.Name = "CyberCircle"
    OCGui.ResetOnSpawn = false
    
    local OBtn = Instance.new("TextButton", OCGui)
    OBtn.Size = UDim2.new(0, 50, 0, 50)
    OBtn.Position = UDim2.new(0, 10, 0.5, -25)
    OBtn.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
    OBtn.Text = "CYBER"
    OBtn.TextSize = 10
    OBtn.Font = Enum.Font.GothamBold
    OBtn.TextColor3 = ACCENT
    OBtn.BorderSizePixel = 0
    Instance.new("UICorner", OBtn).CornerRadius = UDim.new(0, 14)
    
    -- Make draggable
    local dragging = false
    local dragStart = nil
    local startPos = nil
    
    OBtn.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = OBtn.Position
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            OBtn.Position = UDim2.new(
                startPos.X.Scale, startPos.X.Offset + delta.X,
                startPos.Y.Scale, startPos.Y.Offset + delta.Y
            )
        end
    end)
    
    UserInputService.InputEnded:Connect(function()
        dragging = false
    end)
    
    OBtn.MouseButton1Click:Connect(function()
        MainFrame.Visible = not MainFrame.Visible
        OBtn.BackgroundColor3 = Color3.fromRGB(0, 120, 255)
        OBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
        task.delay(0.1, function()
            OBtn.BackgroundColor3 = Color3.fromRGB(10, 10, 15)
            OBtn.TextColor3 = ACCENT
        end)
    end)
end

-- Keybind Handler
UserInputService.InputBegan:Connect(function(input, processed)
    if processed or changingKeybind then return end
    if input.UserInputType ~= Enum.UserInputType.Keyboard then return end
    
    if Keybinds.AutoLeft and input.KeyCode == Keybinds.AutoLeft then
        local ns = not aplOn
        if ns then
            if batAimbotEnabled then stopBatAimbot() updateToggle("Bat Aimbot", false) end
            if aprOn then stopAutoPlayRight() end
            aplOn = true
            startAutoPlayLeft()
        else
            stopAutoPlayLeft()
        end
        updateToggle("Auto Play Left", ns)
    elseif Keybinds.AutoRight and input.KeyCode == Keybinds.AutoRight then
        local ns = not aprOn
        if ns then
            if batAimbotEnabled then stopBatAimbot() updateToggle("Bat Aimbot", false) end
            if aplOn then stopAutoPlayLeft() end
            aprOn = true
            startAutoPlayRight()
        else
            stopAutoPlayRight()
        end
        updateToggle("Auto Play Right", ns)
    elseif Keybinds.BatAimbot and input.KeyCode == Keybinds.BatAimbot then
        local ns = not batAimbotEnabled
        if ns then
            if aplOn then stopAutoPlayLeft() end
            if aprOn then stopAutoPlayRight() end
            startBatAimbot()
        else
            stopBatAimbot()
        end
        updateToggle("Bat Aimbot", ns)
    elseif Keybinds.AntiRagdoll and input.KeyCode == Keybinds.AntiRagdoll then
        local ns = not antiRagdollEnabled
        antiRagdollEnabled = ns
        if ns then startAntiRagdoll() else stopAntiRagdoll() end
        updateToggle("Anti Ragdoll", ns)
    elseif Keybinds.Unwalk and input.KeyCode == Keybinds.Unwalk then
        local ns = not unwalkEnabled
        unwalkEnabled = ns
        if ns then startUnwalk() else stopUnwalk() end
        updateToggle("Unwalk", ns)
    elseif Keybinds.SlowDown and input.KeyCode == Keybinds.SlowDown then
        slowDownEnabled = not slowDownEnabled
        updateToggle("Slow Down", slowDownEnabled)
    elseif Keybinds.Drop and input.KeyCode == Keybinds.Drop then
        task.spawn(doDrop)
    elseif Keybinds.Taunt and input.KeyCode == Keybinds.Taunt then
        if not tauntActive then startTaunt() else stopTaunt() end
    elseif Keybinds.TPDown and input.KeyCode == Keybinds.TPDown then
        task.spawn(doTPDown)
    end
end)

-- Initialize ESP if enabled
if espEnabled then enableESP() end

print("=== CYBER DUELS V2 LOADED ===")
print("Press Z = Bat Aimbot | Press F4 = Taunt")
print("Click CYBER button to open menu")
